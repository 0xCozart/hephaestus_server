mutation createNabuProfile {
  createNabuProfileAlpha(
    input: { content: { userDID: "", displayName: "", accountCreated: "" } }
  ) {
    document {
      accountCreated
      id
    }
    clientMutationId
  }
}
